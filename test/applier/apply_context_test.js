// Generated by LiveScript 1.2.0
(function(){
  var applier, ApplyContext, fixtures, User, Book, RuleRepo, expect;
  require('../test_setup');
  applier = require('../../index');
  ApplyContext = applier.ApplyContext;
  fixtures = require('../fixtures');
  User = fixtures.user;
  Book = fixtures.book;
  RuleRepo = function(){
    return {
      clean: function(){
        return this;
      },
      clear: function(){
        return this;
      },
      can: {},
      cannot: {}
    };
  };
  expect = require('chai').expect;
  describe('ApplyContext', function(){
    var book, ruleRepo, rules, createRepo;
    rules = {};
    createRepo = function(){
      return new RuleRepo('static repo').clear();
    };
    return describe('create ApplyContext', function(){
      context('with no repo', function(){
        return specify('throws error', function(){
          return expect(function(){
            return new ApplyContext;
          }).to['throw'];
        });
      });
      context('with a valid repo', function(){
        return specify('creates with the repo', function(){
          return expect(function(){
            return new ApplyContext(createRepo());
          }).to.not['throw'];
        });
      });
      return context('ApplyContext with valid repo', function(){
        var ctx;
        before(function(){
          return ctx = new ApplyContext(createRepo());
        });
        describe('ucan', function(){
          return specify('registers new can rule in repo', function(){
            ctx.ucan('edit', 'Book');
            return ctx.repo.canRules.should.eql({
              edit: ['Book']
            });
          });
        });
        return describe('ucannot', function(){
          return specify('registers new cannot rule in repo', function(){
            ctx.ucannot('edit', 'Book');
            return ctx.repo.cannotRules.should.eql({
              edit: ['Book']
            });
          });
        });
      });
    });
  });
}).call(this);
